import { OperatorSymbolPipe } from './operator-symbol.pipe';

describe('OperatorSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new OperatorSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
